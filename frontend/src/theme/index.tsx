import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    primary: {
      main: "#1976d2",
      light: "#42a5f5",
      dark: "#1565c0",
      contrastText: "#fff",
    },
  },
  typography: {
    fontFamily: "DM Sans, Roboto, sans-serif",
  },
});
